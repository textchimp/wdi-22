<%= form_for(peach) do |f| %>
  <% if peach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peach.errors.count, "error") %> prohibited this peach from being saved:</h2>

      <ul>
      <% peach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
